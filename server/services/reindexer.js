/**
 * Reindexer is disabled — files are stored in Firebase Storage, not local disk.
 * Projects are fully persisted in MongoDB and Firebase Storage across restarts.
 */
export const reindexUploads = async () => {};
